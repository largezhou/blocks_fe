import { BezierEdge } from '@logicflow/core'
import { ComponentEdgeModel } from '@/components/event-editor/component-edge-model'

export default {
  type: 'component-edge',
  view: BezierEdge,
  model: ComponentEdgeModel,
}
